import React from 'react';

import { PaymentContainer, PaymentTitle } from './styles';

export default function Payment() {
    return (
        <PaymentContainer>
            <PaymentTitle>
                Formas de pagamento
            </PaymentTitle>
        </PaymentContainer>
    );
}